# `apim/products`

APIM product definitions that map to **consumer tiers**. Products carry the
subscriptions and tier-specific policy (e.g. token-per-minute limits and model
gating), which is how per-consumer, token-based governance is enforced.
